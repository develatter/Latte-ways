# Ways

- Read `MAP.md`, then run `npx ways status` before changing files.
- Obey the active mode and state; never skip or edit a gate.
- Keep commits atomic and use `scripts/check.sh` before finishing.
- Reconcile durable discoveries with `.ways/knowledge/`.
- Do not edit package-managed harness files.
