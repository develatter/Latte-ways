---
description: Search repository memory without changing anything
usage: <terms>
---
Run `npx ways query $ARGUMENTS` and answer from the hits, citing their paths. Treat memory as a lead, then confirm against the code before asserting anything.
