---
description: Cancel the active quick work or abandon the active plan
---
Run `npx ways status --json`. For quick work run `npx ways quick cancel`; for a plan run `npx ways plan abandon`. Never cancel SDD work this way; explain that SDD requires `ways repair`.
